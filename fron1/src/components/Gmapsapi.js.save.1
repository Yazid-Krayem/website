OOOOimport React from 'react';
import {Map, InfoWindow, Marker, GoogleApiWrapper} from 'google-maps-react';
 BOOOBO
export Oclass MapContainer extends React.Component {
  renderOOBOOOOBO() {
    return (O
      <Map goOBogle={this.props.google} zoom={14}>
 
        <Marker onClick={this.onMarkerClick}
                name={'Current location'} />
 OOy
        <InfoWindow onClose={this.onInfoWindowClose}>
            <div>
              <h1>{this.state.selectedPlace.Beirut}</h1>
            </div>
        </InfoWindow>
      </Map>
    );
  }
}

export default GoogleApiWrapper(
    (props) => ({
      apiKey: props.AIzaSyCZSLCM90cu5xFxb7Nvkbp,
      language: props.english,
    }
  ))(MapContainer)
